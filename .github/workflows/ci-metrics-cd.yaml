---
name: CI metrics CD

on:
  push:
    branches:
      - main
    paths:
      - tools/ci-metrics
    tags:
      - "tools/ci-metrics/v[0-9]+.[0-9]+.[0-9]+**"
  pull_request:
    paths:
      - tools/ci-metrics/workflows/cd.yaml
      - .github/workflows/ci-metrics-cd.yaml
      - .github/workflows/reusable-cd.yaml

jobs:
  release:
    uses: ./.github/workflows/reusable-cd.yaml
    permissions:
      contents: write
      packages: write
    with:
      tool-directory: ./tools/ci-metrics
